<h1>Lista de Marcas</h1>
 
<table>
  <tr>
    <th>Nombre</th>
    <th>Acciones</th>
  </tr>
 
  <% @marca.each do |marca| %>
    <tr>
        <td><%= marca.nombre %></td>
      <td><%= link_to 'Show', marca_path(marca) %></td>
    </tr>
  <% end %>
</table>